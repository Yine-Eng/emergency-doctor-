import { Text, View } from 'react-native';

export default function SavedReports() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Saved Reports Screen</Text>
        </View>
    );
}

import { Text, View } from 'react-native';

export default function AIHelper() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>AIHelper Screen</Text>
        </View>
    );
}
